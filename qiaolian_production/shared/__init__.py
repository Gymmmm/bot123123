"""Shared production modules copied from the frozen production SHA.

These modules are still imported by more than one extracted runtime. They are
kept byte-identical to production first; later commits may split individual
functions only after dependency and regression checks.
"""
