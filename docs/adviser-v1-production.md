# 侨联说 V1（2026-09-15）

只在 User Bot 的「📋 租赁详情」展示，频道正文不增加该栏目。
引擎仅返回句子，详情负责 `💬 侨联说` 标题和 `•`，最多两条。
没有有效信号时整块隐藏，不显示暂无建议或兜底文案。

唯一引擎：`v3_core/adviser_copy.py`。白名单过滤显式信号，物业、网络
不参与判断；普通泳池、健身房等公共配套不能单独触发栏目。
保洁频率只留最强一条；优先保留全新未入住、私人泳池、河景等精准信号。
数字楼层不推断高低，旧版未标注来源的高低楼层信号不用于建议。
新采集明确的高低楼层信号带 `adviser_signals_version=explicit-v1`。

详情只读取已发布包中的 frozen canonical facts，不读实时事实替换历史证据。
旧包的旧建议文案不沿用，按 frozen facts 重新生成。新包冻结 V1 纯句子。
预生成与详情统一以稳定 public listing ID（缺失时 listing ID）为 seed，
不将事实 hash 拼入 seed。本次不重新编辑频道帖子，不改房态、预约、线索。

本地完整 V3 回归：584 passed，36 条既有 Pillow 弃用警告。
专项测试包含用户指定 10 个边界、100 次确定性、详情 0/1/2 条排版和
旧包禁用历史推断楼层、物业兜底的回归。

上线维护：候选 SHA 的两项现有 Actions 检查通过后，沿用不可变 release、
preflight、原子 current 指针、四服务健康和失败恢复流程。
不创建数据库备份、不迁移数据；保留上一生产 release 和 d3639402 回滚点。
生产验收须读取真实发布包，验证详情栏目数量和数据不变。
测试通过或服务在线不等同于本轮手机点击、预约转化验收。
