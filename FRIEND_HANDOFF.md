# 侨联地产 Bot 使用交付说明

这是给实际运营人员看的唯一入口，不需要理解代码。

## 这套系统包含什么

1. 用户找房 Bot：客户找房、收藏、预约、咨询、老客服务。
2. 管理发布 Bot：预览房源、审核、安排时段、发布到频道。
3. 采集与整理：把来源房源整理成统一稿件和图片。
4. 后台管理：查看和维护发布数据。

## 日常只需要做什么

- 客户端：把用户 Bot 和频道入口发给客户。
- 发布端：在管理 Bot 查看 `/pending`，确认房源后进入发布队列。
- 需要人工跟进时：查看咨询和预约记录，及时联系客户。
- 遇到异常：不要重复启动多个 Bot，把时间和现象发给维护人。

## 朋友验收清单

- [ ] 给用户 Bot 发 `/start`，能看到找房入口。
- [ ] 按区域和预算筛选，能看到房源或正确的无结果提示。
- [ ] 收藏一套房源后能再次查看。
- [ ] 点击咨询，顾问能收到并识别房源。
- [ ] 创建一次现场或视频看房预约。
- [ ] 从频道房源按钮能跳回用户 Bot。
- [ ] 管理 Bot 的 `/pending` 能看到待审核房源。
- [ ] 发布一套专门准备的测试房源，图片、价格、按钮和评论区都正确。

## 当前测试方式

Gym 已经新建测试 Bot 和测试频道。当前先用测试环境验证效果，不依赖旧 Bot：

1. 用户从测试频道点进测试 Bot。
2. 完成一次找房筛选。
3. 打开一套测试房源。
4. 点击咨询或预约。
5. 管理端确认能识别用户和对应房源。
6. 发布一套测试房源，检查图片、价格、按钮和评论区。

测试数据可以是模拟房源，但按钮、跳转、入库和顾问承接必须真实可验证。

## 责任边界

- Bot 最终通过 BotFather 永久转让给朋友，频道/群组和经营数据由朋友控制。
- 转让前朋友必须先主动打开并使用过该 Bot；转让后朋友拥有完整控制权，包括重新生成 Token 或删除 Bot。
- 总助理负责把 Bot 加入朋友的群组/频道、配置所需管理员权限并完成测试。
- Token、密码和验证码不通过普通聊天长期保存。
- 正式频道发布内容由经营方确认；测试频道允许反复修改和验证。
- 功能改造与日常运营分开记录，避免无限维护。

## 最终交付顺序

1. 在 Gym 的测试 Bot 和测试频道把效果调满意。
2. 朋友用自己的 Telegram 账号打开测试 Bot 并发送一次 `/start`。
3. 把 Bot 加入朋友的群组/频道，并只授予运行所需权限。
4. 用测试房源完成频道发布、按钮跳转、咨询和预约验收。
5. 在 BotFather：`/mybots` → 选择 Bot → Transfer Ownership。
6. 朋友接收所有权后重新生成并安全保存自己的 Token。
7. 更新运行环境并做最后一次验收。
